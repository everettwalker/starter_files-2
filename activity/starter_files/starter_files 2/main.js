// 1.
// Define a function max() that takes two numbers as arguments and returns the largest of them. Use the if-then-else construct available in JavaScript.
// Then, write and example of using the function.

function max(num1,num2){
    // Your answer here
     if (num1 > num2){
       return true;
     }else if(num2 > num1)
        return false;

     console.log(max(10,5)===true);

// 2.
// Define a function maxOfThree() that takes three numbers as arguments and returns the largest of them.
// Then, write and example of using the function.

function maxOfThree(num1,num2,num3){
    // Your answer here
    If (num1 > num2) && (Num1 > num3);
         return true;
       }else if (num2>num3) && (num2<num1);{
          return true;
      }else if (num3<num1) && (num3<num2);{
           return true;
      }
console.log ("this is the max of three numbers");
}
maxOfThree(10,5,0);


// 3.
// Write a function that takes a character (i.e. a string of length 1) and returns true if it is a vowel, false otherwise.
// Then, write and example of using the function.

function isVowel(char){
    // Your answer here
    if(isVowel==='a'||'e'||'i'||'o'||'u');

            return true;
}    else if(isVowel===! 'a'||'e'||'i'||'o'||'u';{
             return false;
}
     console.log(e);

// 4.
// Write a function called `sum` that takes two parameters and returns the sum of those 2 numbers.
// Then, write and example of using the function.
   function sum (number1, number2) {
  number1 + number2;
   }
   let (addition)=sum(7,3)
   console.log(addition);


// 5.
// Write a function named `avg` that takes 3 parameters and returns the average of those 3 numbers.
// Then, write and example of using the function.
function avg(num1,num2,num3) {
  (num1+num2+num3)/3;
  }
  let (average)=avg(15,5,10)
  console.log(average);

// 6.
// Write a function called `getLength` that takes one parameter (a string) and returns the length
// Then, write and example of using the function.



// 7.
// Write a function called `greaterThan` that takes two parameters
// and returns `true` if the second parameter is greater than the first.
// Otherwise the function should return `false`.
// Then, write and example of using the function.
function greaterThan (parameter1, parameter2){
if (parameter2 > parameter1);
return true
}if (parameter1>=parameter2);{
  return false;
}


// 8.
// Write a function called `greet` that takes a
// single parameter and returns a string that
// is formated like "Hello, Name!" where *Name*
// is the parameter that was passed in.
// Then, write and example of using the function.
    function greet (name){
      console.log('hello', + name);
    }
      greet ("John");

// 9.
// Write a function called `madlib` that takes 4 or more parameters (words).
// The function should insert the words into a pre-defined sentence.
// Finally the function should return that sentence.
// Note: When I say words and sentence I mean strings. For example:
// words: "quick", "fox", "fence"
// sentence: "quick brown fox jumps over the fence"
// Then, write and example of using the function.
     function madlib (name,event){
       (name,event)
     }
      console.log(Yesterday + 'name' + started + 'event');
      madlib ('James Carr, a new job.');
